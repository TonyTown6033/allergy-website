# 履约功能文档模板

复制这份模板后，将文件重命名为：

- `<feature>-design.md`

如果后续细化，再补：

- `<feature>-api-contract.md`
- `<feature>-test-plan.md`
- `<feature>-migration.md`

---

## 1. 范围

这份文档定义：

- 这个履约功能解决什么问题
- 涉及哪些后台页面、状态动作、权限与日志

这份文档不定义：

- 不在本次实施范围内的周边能力

## 2. 背景

- 当前现状：
- 现有缺口：
- 为什么现在要做：

## 3. 已锁定决策

- 已经确认的业务规则
- 已经确认的页面位置
- 已经确认的不做事项

## 4. 目标流程

- 入口页面：
- 操作人：
- 触发动作：
- 成功后的状态变化：
- 失败或异常时的处理：

## 5. 数据与状态

- 需要新增或复用哪些字段
- 需要新增哪些时间线事件
- 是否需要独立备注 / 审计实体

## 6. API / 页面影响

- 后端接口：
- 前端页面：
- 权限校验：

## 7. 验收标准

- 至少 5 到 10 条可以直接测试的验收语句

## 8. 后续拆分

- 是否需要补 `api-contract`
- 是否需要补 `test-plan`
- 是否需要补 `migration`
